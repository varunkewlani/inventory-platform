server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # React Router client-side routing: unknown paths fall back to index.html
    # rather than a raw nginx 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
